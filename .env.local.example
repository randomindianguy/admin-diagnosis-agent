# Copy to .env.local and fill in. .env.local is gitignored — never commit keys.

# Anthropic — diagnosis tool-use (step 6). Same key family as chunk 1's eval/.env.
ANTHROPIC_API_KEY=

# Voyage AI — channel-1 runbook embeddings (CHUNK2-DESIGN-DECISIONS Q8).
# Free tier covers the capstone. Get a key at https://dashboard.voyageai.com
VOYAGE_API_KEY=
