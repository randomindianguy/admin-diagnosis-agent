{
  "name": "admin-diagnosis-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chunk 1 ruler — validates the eval against a good and a confidently-wrong agent output for Seed 1.",
  "scripts": {
    "validate": "tsx src/validate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
